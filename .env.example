# Verified immutable IPI Card Vault CosmWasm code ID on ipi-testnet-1.
# This overrides the built-in Testnet default when explicitly set.
IPI_CARD_VAULT_CODE_ID=2

# Optional HTTPS endpoint overrides.
# IPI_COMET_ENDPOINT=https://rpc-testnet.ipi.io
# IPI_REST_ENDPOINT=https://rest-testnet.ipi.io
# IPI_EVM_ENDPOINT=https://evm-rpc-testnet.ipi.io
